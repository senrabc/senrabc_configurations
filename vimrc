" Allow mac to use backspace to delete text that WAS NOT created during current
"   session
set backspace=2

" turn on syntax highlighting
syntax enable

" turn on line numbers
set number

" show the command in the bottom bar
set showcmd

" turn on column 80 marker
set colorcolumn=80
